# Migration `20200418200807-more-fields`

This migration has been generated by Salvatore at 4/18/2020, 8:08:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Server" DROP COLUMN "serverId";

ALTER TABLE "public"."ServerLeaderboardEntry" DROP COLUMN "serverId",
ADD COLUMN "serverId" integer  NOT NULL ;

ALTER TABLE "public"."ServerStat" DROP COLUMN "Duration",
DROP COLUMN "totalBytesRecieved",
ADD COLUMN "duration" integer  NOT NULL ,
ADD COLUMN "totalBytesReceived" integer  NOT NULL ;

ALTER TABLE "public"."ServerLeaderboardEntry" ADD FOREIGN KEY ("serverId")REFERENCES "public"."Server"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200418141737-fleshed-out-fields..20200418200807-more-fields
--- datamodel.dml
+++ datamodel.dml
@@ -1,25 +1,27 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
+  binaryTargets = ["native"]
 }
 model User {
-  id        Int      @default(autoincrement()) @id
-  createdAt DateTime @default(now())
-  anonymous Boolean
-  username  String   @unique
-  passwordHash  String?
+  id           Int      @default(autoincrement()) @id
+  createdAt    DateTime @default(now())
+  anonymous    Boolean
+  username     String   @unique
+  passwordHash String?
 }
 model ServerLeaderboardEntry {
   id                 Int              @default(autoincrement()) @id
   createdAt          DateTime         @default(now())
-  serverId           String
+  serverId           Int
+  server             Server           @relation(fields: [serverId], references: [id])
   score              Int
   leaderboardEntryId Int
   leaderboardEntry   LeaderboardEntry @relation(fields: [leaderboardEntryId], references: [id])
 }
@@ -47,9 +49,8 @@
 }
 model Server {
   id        Int      @default(autoincrement()) @id
-  serverId  String
   createdAt DateTime @default(now())
   updatedAt DateTime @default(now())
   serverUrl String
   live      Boolean
@@ -65,13 +66,13 @@
   users              Int
   spectators         Int
   entities           Int
   messages           Int
-  Duration           Int
+  duration           Int
   bytesSent          Int
   bytesReceived      Int
   totalBytesSent     Int
-  totalBytesRecieved Int
+  totalBytesReceived Int
   boardWidth         Int
   memHeapUsed        Int
   memHeapTotal       Int
   memExternal        Int
```


